# ============================================================
# .env.example — 完整环境变量参考（本地开发用 .env.local）
#
# 生产环境：所有敏感值在 GitHub → Settings → Secrets → Actions 中管理
# 本地开发：复制为 .env.local 并填入真实值
# ============================================================

DOCKERHUB_USER=clintonluo
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=openpanda
JWT_SECRET=change-me-to-a-strong-secret-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# ── AI 供应商（可选，也支持管理后台页面在线配置）──
DEEPSEEK_API_KEY=
QWEN_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
