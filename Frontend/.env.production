# ============================================================
# .env.production — 前端生产环境（Vite 自动加载）
#
# 何时生效:
#   npm run build → Vite 自动读取此文件（Docker 构建时执行）
#
# 注意:
#   - API 地址用相对路径 /api/v1（由 nginx 代理到后端）
#   - 不是机密文件，可提交到 Git
# ============================================================

# API 基础地址（生产环境 nginx 代理 /api/ → backend:8080）
VITE_API_BASE_URL=/api/v1

# 应用标题
VITE_APP_TITLE=开源熊猫 技术交流平台
